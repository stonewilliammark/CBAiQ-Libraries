# Get Set Up & Access

Prompts for setting up AI tools, building AI projects, and getting access to platforms and services.

Each subfolder is a separate prompt — add new prompts by creating a new subfolder with a `README.md` and at least one version file (e.g. `prompt_v1.0.md`).
