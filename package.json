{
  "name": "cbaiq-libraries",
  "version": "1.0.0",
  "description": "CBAiQ Libraries & Solution Catalogue — content repo and GitHub Pages site",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && echo 'Build complete — open site/index.html'"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
